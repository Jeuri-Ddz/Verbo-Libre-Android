package com.mi.bibliarv1960

/**
 * Configuración de depuración para forzar comportamientos específicos en la app.
 * Cambiar estos valores solo para pruebas locales.
 */
object DebugConfig {
    /**
     * Si no es null, fuerza la carga de un devocional específico por su ID del JSON.
     * Ejemplo: const val DEBUG_FORCE_DEVOTIONAL_ID: Int? = 5
     */
    val DEBUG_FORCE_DEVOTIONAL_ID: Int? = 1
}
